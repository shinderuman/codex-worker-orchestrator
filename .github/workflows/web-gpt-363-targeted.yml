name: Web GPT 363 Targeted Validation

on:
  push:
    branches:
      - web-gpt/363-orphan-watch-lease-revalidation

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - name: Read quality tool contract
        id: quality-tools
        run: |
          GO_VERSION=$(awk -F': ' '$1 == "go" { print $2 }' quality-tools.yml)
          printf 'go_version=%s\n' "$GO_VERSION" >> "$GITHUB_OUTPUT"
      - uses: actions/setup-go@v7
        with:
          go-version: ${{ steps.quality-tools.outputs.go_version }}
          cache: false
      - name: Go format check
        run: |
          output=$(gofmt -l glm-worker/internal/app/watch.go glm-worker/internal/app/watch_orphan_unix_test.go)
          test -z "$output" || { printf '%s\n' "$output"; exit 1; }
      - name: App tests
        working-directory: glm-worker
        run: go test ./internal/app
