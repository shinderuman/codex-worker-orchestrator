name: Web GPT 377 Run
on:
  push:
    branches:
      - web-gpt/377-architecture-responsibility-audit
permissions:
  contents: write
jobs:
  validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Apply contract update
        run: |
          python3 .github/web-gpt-377-contract.py
          gofmt -w glm-worker/internal/harnesslint/quality_surface.go glm-worker/internal/harnesslint/quality_surface_test.go glm-worker/internal/app/command_test.go
          rm -f .github/web-gpt-377-contract.py
          rm -f .github/workflows/web-gpt-377-contract-validate.yml
          rm -f .github/workflows/web-gpt-377-run.yml
          git diff --check
          git config user.name web-gpt
          git config user.email web-gpt@users.noreply.github.com
          git add -A
          git commit -m 'Decouple quality wiring gate from workflow filename'
          git push
      - name: Read Go version
        id: quality-tools
        run: |
          GO_VERSION=$(awk -F': ' '$1 == "go" { print $2 }' quality-tools.yml)
          printf 'go_version=%s\n' "$GO_VERSION" >> "$GITHUB_OUTPUT"
      - uses: actions/setup-go@v7
        with:
          go-version: ${{ steps.quality-tools.outputs.go_version }}
          cache: false
      - name: Install quality tools
        run: |
          quality_bin="$(go env GOPATH)/bin"
          QUALITY_TOOLS_BIN_DIR="$quality_bin" ./install-quality-tools.sh
          printf '%s\n' "$quality_bin" >> "$GITHUB_PATH"
      - name: Targeted tests
        working-directory: glm-worker
        run: go test ./internal/app ./internal/workflow ./internal/harnesslint
      - name: Repository lint
        run: ./harnesslint
      - name: Full tests
        working-directory: glm-worker
        run: go test ./...
      - name: Vet
        working-directory: glm-worker
        run: go vet ./...
      - name: Build
        working-directory: glm-worker
        run: go build ./...
