name: Web GPT 377 Final Validation
on:
  push:
    branches: [web-gpt/377-architecture-responsibility-audit]
permissions:
  contents: write
jobs:
  validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Remove temporary edit and validation machinery
        run: |
          rm -f .github/web-gpt-377-edit.py .github/workflows/web-gpt-377-edit.yml .github/workflows/web-gpt-377-validation.yml
          git config user.name web-gpt
          git config user.email web-gpt@users.noreply.github.com
          git add -A .github
          git commit -m 'Remove temporary #377 work machinery'
          git push
      - name: Read quality tool contract
        id: quality-tools
        run: |
          GO_VERSION=$(awk -F': ' '$1 == "go" { print $2 }' quality-tools.yml)
          printf 'go_version=%s\n' "$GO_VERSION" >> "$GITHUB_OUTPUT"
      - uses: actions/setup-go@v7
        with:
          go-version: ${{ steps.quality-tools.outputs.go_version }}
          cache: false
      - name: Install quality tools
        run: |
          quality_bin="$(go env GOPATH)/bin"
          QUALITY_TOOLS_BIN_DIR="$quality_bin" ./install-quality-tools.sh
          printf '%s\n' "$quality_bin" >> "$GITHUB_PATH"
      - name: Targeted architecture tests
        working-directory: glm-worker
        run: go test ./internal/app ./internal/workflow
      - name: Repository lint gate
        run: ./harnesslint
      - name: Full Go test suite
        working-directory: glm-worker
        run: go test ./...
      - name: Full Go vet
        working-directory: glm-worker
        run: go vet ./...
      - name: Full Go build
        working-directory: glm-worker
        run: go build ./...
