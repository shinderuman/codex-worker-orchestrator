# codex-worker-orchestrator 実装履歴

このファイルは、完了証跡とescaped bug/reviewの原因分析を保持するtracked archiveである。現在状態・優先順・次の操作の正は`IMPLEMENTATION_PLAN.local.md`とし、この履歴は通常の作業開始・再開時には全文を読まない。

次の場合だけ、関連見出しまたは該当語を検索して必要範囲を読む。

- 完了済み項目のfalse-complete棚卸しまたは再オープン
- escaped bug/reviewの原因層分析
- 過去commit・検証・本配置証跡の確認
- 現在taskが過去対策と同じinvariantへ触れる場合

本文を更新できるのは親Codexだけとする。GLM worker/reviewerは読み取り専用で参照し、必要な更新候補と根拠をPACKETへ記載する。

## 重大インシデント / 再オープン中

- [x] source comment absolute invariant: 過去のsemantic exception方式を撤去し、Go scanner・shell lexer・TOML/rules/.gitignore分類、exact build constraint/shebang allowlist、未分類source fail closedを備えたrepository専用`commentlint`と安全・冪等な`--fix`を追加。tracked/untracked sourceから4,038件の自然言語commentを除去して0 violationへ収束し、workflowのreviewer前mandatory gate、installer preflight、installed binary、self-protection、production-path negative testへ統合した。親Codexが直接実装・semantic audit・全Go test/race/vet/build・shell syntax・full install smoke・最終reviewを行い、GLM worker/reviewerは使用していない
- [x] safe-stop/isolation external-review follow-up: 公開済み実装の4 findingをcurrent production codeで再検証し全件成立と確定。process group残存時は`interrupted_cleanup_residual`へ安全停止authorityを分離し、隔離branch統合後の非parent source deltaを拒否、停止dirty identityへmode/type/conflict stageを追加、endpoint Closeで受理済みhandlerのack書込み完了を待つ構造へ修正した。Finding 1は明示済みgroup非残存contractをimplementation・GLM自己review・独立review・Sol・親final gateが逃した層、Finding 2/4はprovenance closure/server lifecycle test gap、Finding 3は上位保持contractのedge明示不足とlossy fingerprint/review gapの合成として記録。決定論的regression、全test/race/vet/build/gofmt、独立review、Sol最終採否、parent accept、final HEAD gate、本配置、source/installed一致、production smokeを完了
- [x] external feasibility dispatch gate: ACTIVE taskの`## External feasibility`宣言をnew/decision/fix/reviewer/auto-fix/resumeの全model dispatch入口で単一parserにより検証し、missing・malformed・実producer evidence/親Go判断欠落をmodel呼出0回でfail closed化。PoC/observationはread-only capabilityと開始前後HEAD/index/worktree snapshot同一性でproduction diffを禁止し、結果をreviewer完了へ流さず親Go/No-Goへ戻す。拒否時checkpoint/status/session/pending decision保持、fenced Original instruction境界、PoCからimplementation宣言migration後のwritable継続をreview follow-upで固定した。全test/race/vet/build/gofmt、独立review、Sol最終採否、parent accept、final HEAD gate、本配置、source/installed一致、production smokeを完了。宣言内容のsemantic真偽は親Codex責務として残し、追加AI classifierは導入していない
- [x] glm-worker safe-stop production integration: 実Claude CLI 2.1.226のprocess-group停止・descendant消滅・partial session resume PoCを前提に、repo-lock ownerが単一目的0600 unix socketで固定`--stop`要求を受理し、Claude process groupへTERM→10秒上限後KILL、direct child wait・group非残存、`interrupted` checkpoint/status保存後にmachine ackする経路を実装。停止元invocationはstructured `kind: interrupted` + non-zeroでPASS/completeを抑止し、`--resume`で同一stage/phase/sessionを継続する。初回call前の未採番sessionは新規sessionとしてresumeし、task statusを7値+nullへ同期。実binary process series、worker/reviewer/tool child、停止race、endpoint absent/stale、TERM無視child、全test/race/vet/build/gofmt、独立review、親Codex差戻し後再review・最終採用、final HEAD gate、本配置、installer flags source/installed binary SHA-256一致、managed instruction一致、owner不在`--stop`のstructured `stop_endpoint_absent` + non-zero・task ID/status不変smokeを完了
- [x] completion detection反復false negative重大インシデント: Task 007 task ID `173436e3-633c-493d-a6c7-9816704f0888`でproducer raw stdout・telemetry reviewer result・親Codex tool outputは各1件、ユーザー可視表示は2件と層別確定。Codex model/永続contextへの二重流入証拠はなくactual token影響はunknownのため、Desktop二重描画自体は最上位Evalへの実害未立証なBLOCKED外部境界として非対応とした。重大インシデント本体は、1回目に完了条件をrepo内emitと原因境界特定へ狭め、2回目に模擬test・instruction・単発live positiveを継続的production enforcementと誤認したcompletion detectionの反復失敗。obsoleteな完了証拠をEVAL/instructionから撤回し、要求充足と「要求違反は残るが上位目的への実害なしで非対応」を分離。独立review・Sol採否・parent accept・follow-up後HEADへのstash復元とmanifest pin再同期、final HEAD gate、本配置、managed instruction source/installed hash一致を完了
- [x] Task 007 machine JSON contract external-review follow-up: status/status statsの観測不能sentinelを外部JSON nullへ正規化し、非active `task_liveness`も常時key+nullへ統一。watchはENOENTだけをempty/removed正常経路とし、他のI/O失敗をstructured internal error + non-zeroへ分離。stats全map集計fieldをzero-dataでもJSON objectへ固定し、README timeline旧graph記述を削除した。raw JSON・決定論的非ENOENT・実process境界test、全test/race/vet/build/gofmt、独立review、Sol採否、parent accept、final HEAD gate、本配置、installed `--status` null・`--stats`全map object smoke、installer同一flags buildとのbinary byte一致を完了

## Escaped bug/review原因分析

- source comment再増殖: 大幅cleanup `ab9c69d`後、`5365957`と`ca60324`で「原則禁止だが理由・不変条件・security・compatibility等は許可」というsemantic ruleをworker/reviewerへproduction配線していた。GLMはruleを読んでいたが例外を広く解釈し、同種modelのreviewerも同じcommentを有用と判断できた。一度commitされたcommentは後続のdiff中心reviewから外れ、repository-wideな絶対machine postconditionがなかったため累積した。対策はprompt強化でなく、自然言語commentの受理authorityをexact allowlistの`commentlint`へ単一化し、workflow・installer・full corpus testで0 violationを強制する構造へ置換した
- safe-stop本配置前smokeの親orchestration誤起動: 親Codexが最初の`install.sh`主process sessionを保持せずpreflight途中で完了扱いし、source/installed一致確認と`glm-worker --stop` smokeを並行実行した。旧installed binaryは未知の`--stop`をunknown optionとして拒否せず通常instructionへfallbackしたため、新ACTIVE taskをUSER_REQUEST=`--stop`で1 call誤起動した。workerはinstalled/source不一致・旧parser fallback・working tree変更なしを検出して`NEEDS_SOL_DECISION`で停止し、変更混入は防止した。対策はinstall主processを終端まで保持し、source/installed hash一致を確認した後に新command smokeを逐次実行する既存install contractの遵守であり、fixtureやworker判断を本配置完了の代替にしない
- safe-stop重複request outcome待機不整合: 親設計は`--stop`の固定request/ackと停止完了authorityを定義したが、同時重複requestのwaiter集合を具体化しなかった。実装は`Request()`を冪等化した一方、確定outcomeを単一buffered channelへ1件だけ送ったため、通知前に複数handlerが待機すると1件以外がhandshake timeoutまで停止し得た。GLM自己review・独立reviewは単一requestと通知後の再観測だけを確認し、親Codex最終reviewがproducer/consumerの複数waiter受理集合を検出した。対策は個別timeout調整でなく、最初のoutcomeをmutex下へ保存しdone channel closeで全waiterへbroadcastする構造とcontroller/endpoint同時request testへ収束した
- task status machine enum残存漏洩: 親external-review指示は`TaskStatusNone`/`unknown` presentation leakageと有限集合整合を要求したが、永続`task.status`の任意非空値とstatus/stats以外の同一field producerを具体的に列挙しなかった。初回実装・testはNoneと既知stateへ局所化し、reviewもproducer横断の受理集合を照合しなかった。follow-up workerはstatus/statsを6値+nullへ修正し、独立reviewがtimeline/convergenceの残存経路をcommit前に検出。Sol差戻しで4 commandを同じhelper・raw JSON contractへ収束し、`watch_exit.status`は`task-switched`を含む別domainとしてscope外を明示した
- Z.ai 5時間limit early-stop false-complete: 親Codex orchestrationのexternal feasibility gate不適用と人工fixtureのauthority誤認。production価値はClaude CLIのretry途中eventへexact `[1308]`・5h・reset時刻が出ることへ全面依存したが、親は実producer PoC前にimplementationをdispatchし、worker/reviewer/Solもexact本文を直接与えるfixtureを実event schema/timingの成立証拠として受理した。実`claude -p`ではretry attempt 1-10はgeneric 429だけで、exact signalは全retry後のterminal assistant/resultに初めて出るため、early-stopは時間を短縮せず直後のresultを失わせた。Task 015の文書完了条件だけでは親dispatchを止めないため、再発防止は実producer evidenceと親Go判断なしのimplementation model callを0回で拒否するproduction dispatch gateへ分離した
- Z.ai 5時間limit即時停止の初回Sol差戻し: requirement scopeとprocess I/O合成漏れ。初回実装はexact signal検出とkillを追加した一方、descendant pipe保持対策として`command.WaitDelay=5s`を全runへ無条件適用し、正常runでも成功result後のpipe保持を新たなerrorへ変え得た。また早期観測をstderr/plain stdoutへ限定し、既にdecodeしているJSON stream event内のexact signalを終端まで待つ境界が残った。独立reviewが残余riskを明示し、Solが「5h limitだけ」のscope違反として差戻した。対策は全run timeoutを撤去し、stderr/plain/JSON eventを同じ`DetectZaiFiveHourLimitText`・同じstopperへ統合、limit検出runだけ5秒bounded drain、非limit runはEOFまで従来どおり待つ構造とproduction-path testへ収束した
- completion detection反復false negative: 親Codex orchestrationのauthority/evidence gap。第1回は「ユーザー可視1回」をrepo内emit 1件とcaller境界特定へ狭め、第2回はfixed Eval・instruction contract・一度のlive positiveを実Desktop rendererの継続的production enforcementへ格上げした。worker/reviewer/Solは与えられた狭い完了証拠の内部整合を確認したが、要求・観測可能postcondition・証拠authorityの差とrepo外表示境界を横断しなかった。対策は表示dedupeやchecklist追加ではなく、層別evidence、actual Codex impact gate、単発観測を完了証拠にしないauthority区別、repoで強制不能な要求違反の非対応/BLOCKED分類へ収束した
- Task 007 machine JSON残存不整合: 親指示・旧挙動移植・有限集合reviewの合成漏れ。親Task 007はpresentation sentinel削除、structured error + non-zero、JSON type維持を要求した一方、内部`TaskStatusNone`/`LockUnknown`の流出経路、watchの`err != nil`をENOENTとその他へ分ける監査、zero-data raw nil map検査までは具体化していなかった。実装は旧sentinelとlog不在分岐を一部JSONへ形式移植し、typed testの`len(nilMap)==0`がnullと`{}`を区別しなかった。reviewer/SolもREADME/Codex instruction/producerのenum集合、正常不在と非ENOENT failure、zero-data raw JSON型を横断照合しなかった。対策は外部boundaryのnull/object正規化、ENOENT限定分岐、raw JSON/process境界testへ収束し、generic schema/fs frameworkは追加していない
- `sol_question` status contract validation不整合: 親指示・derived contract・実装・review/Sol gateの横断漏れ。Task 006親指示は`sol_question`のfield audit、status別contract、schema/validator acceptance一致を要求したが、全status text fieldを同一validation sourceへ収束するところまでは具体化せず、実装はPASS/FIX_REQUIRED noise除外をstatus special-caseで修正したため`sol_question`だけ共通`contractField`の外に残った。reviewer/Sol gateもmachine JSONのstatus-scopingを確認する一方、status tableの全契約text field共通制約とvalidator受理集合を照合しなかった。対策はnewline checkの個別追加でなく、NEEDS_SOL_REVIEW固有field集合を共通reviewer fieldから構成し、validator・MachineJSON・Displayを同じ`contractFields()`へ収束すること
- parent review origin semantics不整合: requirement preservation / reviewer terminology / Sol gateの合成漏れ。初回実装は`codex-review`をGLM reviewerの指摘として定義し、Original instructionが必要とした「親Codex最終reviewで新たに検出した差戻し」とGLM reviewer既記載指摘を混在させた。独立reviewは内部整合だけを確認し、`Contract vs Original instruction / Resolved references`の主体差を検出しなかった。Sol reviewで`codex-review`を親Codex新規検出だけへ限定し、GLM reviewer terminal既記載の差戻しを`glm-reviewer`へ分離。CLI・instruction・artifact・集計・production-path testの有限集合を同期した
- PTY startup/feed race見逃し: `glm-worker`内部transport testと親orchestrationの合成漏れ。実PTY testがproductionにないhelper ready-file同期を待ち、live 9-byte検証もtool往復latencyがchild raw化より長かったため、READYなし即writeのraceを隠した。同期除去testで4/4失敗し、raw化前canonical line disciplineによる本文滞留・CR変換・Ctrl-C消費・echo漏洩を確認。対策はTTY raw適用成功後の固定READY markerと、marker確認直後の1回writeをproduction/test/instructionで同一契約へ収束し、旧ready-file・sleep・caller stty・retryを使わない
- task corpus conformance誤検出: test corpus/section境界の失敗。task file全文のliteral scanがlossless Original instruction内の`## Status`やtask path bulletをschedule metadataとして扱い得たが、fixtureが偶然その文字列を含まず検出されなかった。対策は一般Markdown parserを追加せず、fence外のtop-level `## Status` / `## Dependencies` sectionだけを判定する
- MODEL_IDLE非model event reset: observability classification/testの失敗。model activityと全eventの時刻を分離せず、fixtureの最終eventが常にassistantだったためsystem tool_progressによるresetが観測されなかった。対策はassistant thinking/text/tool_useだけの独立時刻を使い、30秒tool_progress中も増加するtestへ固定する
- MODEL_IDLE thinking_tokens不整合（`3828347`のescaped follow-up）: 親指示・requirement preservation・cross-cutting reviewの合成。前回の親レビュー指示がmodel activityとしてassistant thinking/text/tool_useを列挙した際、runnerが既にmodel activityとして扱っていたsystem/thinking_tokensを列挙から落とし、それを受けたtask Contractが「最低限」を「assistant 3種だけ」へ狭めた。workerは契約どおりに実装したが、runnerとwatchをまたぐmodel-activity受理集合の整合を横断確認するreviewが無く、高頻度抑止でevent logへ保存されないthinking_tokensが長時間thinking中もMODEL_IDLEを増やし続けた。対策は個別event名の追加ではなく、受理集合を`state.IsModelActivityEvent`としてproducer/consumer間の単一契約へ一致させ、generic `last_event_at`とmodel activity専用時刻`last_model_activity_at`をlive snapshotへ分離すること
- Task 001/002後のcontract不整合: requirement migration / entrypoint retryability / source-of-truth ownership / parser acceptance-set compositionの横断漏れ。新ACTIVE requirement sourceをproductionへ追加した後もWORKER/REVIEWERの旧USER_REQUEST正本記述を撤去せず、`ExecuteDecision`はACTIVE解決前にdecisionを消費して修復再試行を塞いだ。Planとtask fileへschedule stateを二重保持し、完了task file削除とdependency path保持も合成していなかった。schedule parserは既知`- `だけを抽出して未知list記法をfail-openにした。対策はdecision消費前gate、要求正本prompt置換、runtime/installerの同一fail-closed受理集合、Plan単独schedule stateとfulfilled dependency除去へ収束した
- Task 003 TARGETS element semantic false-complete: `glm-worker`内部worker/reviewer pipelineのrequirement migrationとacceptance-set比較漏れ。旧text protocolの非空TARGETSをtyped arrayへ移す際、field存在と配列長だけを復元完了条件にし、各要素の空白・none混在・case/padding variant・PACKET・重複をstatus横断で比較しなかった。EVALの「NEEDS_SOL_REVIEWはnone要素拒否」とproductionのall-none predicateも不一致だった。対策はworker/reviewer全status共有の`validateTargets`単一predicate、同一tableによる要素受理集合比較、dispatch前補正test、prompt/EVAL/schema comment同期へ収束。Sol reviewで`["none"]`厳密正規形と入力値に依存しないreject categoryまで固定した
- Task 002 final HEAD gateのSol/independent review差戻し: requirement preservationとproducer/consumer acceptance-set比較の横断漏れ。初回実装はACTIVEの正例と既知stale例へ寄り、NEXT/BLOCKEDの全bullet、runtime path配置契約、過渡表現patternのfalse positive、shell/runtime間のbullet抽出規約を同一受理集合として照合しなかった。対策はACTIVE/NEXT/BLOCKED全bulletのfail-closed検証、共通path validator、pending表現へ限定したidentifier境界、runtimeとinstallerのpath・bullet抽出parity test、malformed実Git scenarioへ収束した
- Task 001 production統合のSol差戻し3件: requirement preservation / production wiring / cross-cutting invariant compositionの合成漏れ。初回実装はACTIVE解決失敗を0-callで止めたが、`active-task`未設定と前task残存を継続entrypointへ対応付けず、親修復後の同task再開で要求正本なし・旧task参照を許し得た。review resumeはreviewStart・stop・currentの3状態を比較しながら、呼出中変化後に同一fileが停止中再変更されたcaseを停止中deltaへ誤昇格した。ACTIVE pathはlexical prefixだけを確認し、`.md`拡張子とsymlinkを辿らないregular file条件を配置契約へ機械適用していなかった。対策は未設定/設定済み空値のstate contractと全継続entrypointの再解決、`reviewStart != stop`のcurrent非依存拒否、regular `.md` file限定をproduction-path testで固定する
- Task: Kindle-automationでescapedした4問題について、Codex + GLM連携が事前検出できなかった原因を実ログ・raw telemetry・Git履歴・code・prompt・PACKET contractから分析
- 状態: 分析完了。Kindle task会話、旧schemaを含むraw JSONL、両repositoryのGit履歴、現行worker/reviewer prompt、主要artifact、response保存code、review workflowを照合済み。進行中GLM taskとは独立し、checkpoint/sessionを変更していない
- 確認済みの共通原因: task局所のdiff適合へ寄り親目的と残作業を保持しないtask lifecycle、弱い外部成立性仮説がSPEC化されると再検証されにくいauthority inversion、実行時failure evidenceとreview artifactの境界不一致、review phase遷移を意味変更の有無より優先するreview invocation
- 原因層: 4件はworker/reviewer pipelineの個別チェック不足ではなく、親Codexを含むorchestration contract（critical assumption確定、親USER_REQUEST lifecycle、runtime evidence管理、semantic deltaに基づくreview invocation）の不足。今後のescaped case分析では最初に`glm-worker`内部pipelineの失敗か親Codex orchestrationの失敗かを分類する
- 対策判断: 既存の4対策は各原因へ直接対応している。worker/reviewerへ個別チェックを積み増さず、新しい第5・第6対策も追加しない
- `66b1431`: `glm-worker`内部pipelineの失敗。REVIEWER.md/scenarioは意味情報不足を「再出力」と定義したが、production `automaticFixPrompt()`は実装修正・test再実行を要求してcontractが不一致。scripted runnerが期待packetを直接返すscenarioはproduction promptに従った結果かを検証せず、4 callの状態遷移だけを自己充足的に確認した。今後はexpected終端だけでなく、production dispatch分岐と実際に渡すpromptの禁止・必須contractを直接検証する
- `70533f0` telemetry欠落: `glm-worker`内部pipelineの失敗。provider recoveryの停止状態を表す`recoveryFatal`へ「probe段階fatalでTask Work Call未実行」と「probe成功後のresumed Task Work Call実行後fatal」を畳み、後者のraw telemetry保存まで抑止した。`d460cd7`で追加したTask/Probe加法整合invariantを後続fatal transitionへ横断適用せず、initial transient→probe success→resumed task→ordinary nontransient fatalを検証しなかったことが原因。個別scenario追加だけでなく、call実行事実とrecovery終端分類を独立stateとして扱う
- `70533f0` fatal誤分類: `glm-worker`内部pipelineの失敗。契約はauth/config文脈を持つ限定信号だけをfatalとする一方、production matcherは裸の`unauthorized`/`forbidden`をsubstring一致させ、false positive時にcheckpoint/sessionを破棄する。代表的negative testの存在を対策済み証拠とみなし、production matcherの全列挙値を契約へ照合しなかったことが原因。matcher contractと列挙値の対応、裸の一般語negative case、checkpoint/session保持まで検証する
- `c06f6b2` report-only invariant欠落: `glm-worker`内部pipelineの失敗。前回reviewは通常auto-fixからreport-only prompt/phaseへ分離するroutingを中心にし、worker capabilityと「開始前snapshotからworking tree不変」というproduction postconditionを要求・検証しなかった。prompt文面をcontract enforcementと同一視し、後続reviewが変更後worker-end snapshotを新基準として受理する境界を見落とした。対策は個別prompt追記ではなくreport-only前後の3軸snapshot同一性をorchestratorがfail closedで強制する
- `977f94b` eval-ab read-only違反: `glm-worker`内部pipelineの失敗。A/B評価ロジックとAI call/repo lock禁止は検証したが、production `Execute()`のmode分岐前に`NewStateStore()`がdirectory作成・`repo-root`書込みを行う共通初期化副作用をentrypoint全体で追わなかった。feature本体のread-only宣言を統合後のpostconditionとして検証していない同一上位原因であり、`--eval-ab`をwrite前のattach経路へdispatchしstate非作成をproduction testで固定する
- 実運用PACKET二重表示再現: 親Codex orchestration失敗。元報告後にrepo内二重emit 0件へ問題を狭め、再報告後はCodex desktopのbackground exec live更新と後続wait result cardが同じterminal resultを二面描画する具体的境界を特定したが、原因境界の特定をユーザー可視症状の解消と取り違えて再び完了扱いした。2026-08-20 BM25 taskではalias/function/wrapperなしの本配置binary 1実体、raw telemetryの`NEEDS_SOL_DECISION` response/PACKET 1件、deferred `wait` function_call_output 1件、永続contextへの流入1件を確認した一方、2026-08-21 structured output production taskの`RATE_LIMITED` packetでも同一payload二面表示が再現した。3回の報告を通過した上位原因は、`EVAL.md`がcaller側echoをrepo外として明示除外し、親lifecycle behavioral Evalも未実行の文書contractに留まるため、親Codexが完了条件を「ユーザー可視1回」から「境界特定」へ狭めても機械的に拒否しなかったこと。false-complete棚卸しも同じ狭い完了条件を再利用して自己検証になった。単一対策は「1 accepted terminal resultにつき親tool orchestration全体でユーザー可視payload 1回」という親postconditionをcaller除外なしで固定し、raw background outputの内部保存→短い同期1回取得を追加AI callなしのdelayed markerと実terminal resultで検証すること。repo側blind dedupe・JSON化・worker/reviewer checklistでは代替しない
- `f521ab4` convergence doc taxonomy矛盾: `glm-worker`内部pipeline/review失敗。comment normalizationの個別言語caseへ寄り、taxonomy全体の意味整合を確認せず、同じ実装/test内の「doc全内容がsemantic digest対象」と「doc追記はnon-semantic」の矛盾を見逃した。doc追加・変更・削除をfile種別だけでnon-semanticにせず、`doc-change`独立分類または安全側semanticへ変更し、AGENTS/instructions/EVAL等behavior-critical文書がcomment/doc/format-onlyへ落ちないことを固定する。コードcomment/format-only分類は維持する
- Sol decision伝達途中欠損: 完了。当初欠損は親Codexが`JSON.stringify`本文をshell二重引用符commandへ埋め込み、backtick command substitutionの出力混入と最初のNUL byteでのargv切断でdecision本文の過半が静かに喪失した親Codex orchestration/shell invocation境界の失敗。対策としてstdin専用mode（byte長・任意SHA-256照合・fail closed）と親caller contract・実PTY testを`1dbfda5`/`3c263a6`で固定した。review中に判明した1時間待機は一過性でなく、親Codexがtty:true PTYをcanonical modeのまま使い末尾改行なしpayloadがline bufferへ滞留し、glm-workerがstate初期化前のio.CopyNで待ったもの。04:20完了run後のstale PTYではなく、後続fix runがGLMへ未到達だった。production Go tool defectではないためGo codeは変更せず、instruction（tty:true・`stty raw -echo`・1回write）とcontract/PTY testで対処した
- `--watch`終端後hang: 親Codex orchestration失敗。旧`--watch`はREADMEどおりCtrl-Cまで常駐するtailでauthoritative task statusを参照せず、親Codexが主`glm-worker`呼出processを失った後のterminal waitへ誤用した。対策は通常完了待機を主processに限定し、attach recovery時だけ既存`--watch`を使い、follow対象taskのnon-active/task切替時にstate読後の最終event drainと`WATCH_EXIT`を出してexit 0する単一contract。独立review・Sol検証・本配置後、実`waiting-sol-review` taskで保存済みevent全件後に終了行を出して即時復帰することを確認済み
- `ba2414b` BM25 fingerprint domain不整合: `glm-worker`内部pipeline/review失敗。repository境界を`enumerateFiles()` helperの局所contractとして確認し、production `Search()`のfingerprint→enumeration→rebuild全体で同じcorpus policy・size/binary/symlink/nested repo/submodule/exclude境界を共有するか確認しなかった。対策はfingerprintをtracked/untracked列挙と`readSearchableFile`の同一corpus投影へ収束し、HEAD metadata軸を削除して検索対象の意味変更だけをfreshnessへ反映するcontract。境界とcache hit/rebuildをproduction `Search()` testで固定してcommit済み
- `962fb20` plan guard telemetry欠落: `glm-worker`内部pipeline/review失敗。plan guardのfail-closed状態遷移だけをreviewし、過去の`70533f0`で確立した「実行済みTask Work Callは全terminal pathでraw telemetryへexactly once残す」というcross-cutting invariantを、model call後のafter-read error early returnへ適用しなかった。対策はinitial/resumed/provider recovery、call前停止、mismatch、after-read failure、success/errorを横断するexact-once行列testとし、plan/history両親管理surfaceへ同じguard責務で適用して同commitへ収録済み
- review-resume snapshotと親専有file更新の衝突: 親Codex orchestrationと`glm-worker`内部contractの境界不整合。structured output移行taskのreview-start後、rate-limit待機中に親Codexが契約どおりplan/historyへtelemetry分析と優先順を追記したところ、HEAD/index一致・親管理2fileだけのworktree差分でもreview-resume全worktree同一性がfail closedし、独立reviewを再開できなかった。親fileをmodel call中に不変とするguardと、worker実装surfaceをreview開始前後で不変とするsnapshotは必要だが、親が局所終端ごとにplanを更新するlifecycle contractとの合成を定義していなかったことが原因。対策は親更新を禁止せず、worker/reviewer実装surfaceの外部変更を許容せず、保存済みsnapshotから親管理2fileだけの親承認済みdeltaを識別して新baselineへ移す単一orchestrator contractとする
- structured output status契約と受理集合の脱落: `glm-worker`内部worker/reviewer pipeline失敗。旧text protocolのstatus別required fieldsをtyped schema/validatorへ移す際、reviewer 3 statusとworker `NEEDS_SOL_DECISION`の非空`TARGETS`契約を対応表で照合せず、対象なし結果がauto-fixまたは親dispatchへ進める状態になった。同時に、制限したJSON Schema語彙では未知propertyを許容するproducer contractと、`DisallowUnknownFields`で拒否してrepairなしfail closedするconsumer contractを個別の安全策として評価し、受理集合の合成を確認しなかった。対策はstatus別旧requiredFields対応とproduction dispatch因果、producer schema対consumer acceptanceを機械testで固定する
- tracked canonical plan stale-by-one再発: 親Codex orchestration失敗。初回commit→plan/history同期→同一commit amendという手順を指示したが、final HEADのplanが完了済みcommitを「amend直前」「install前」と記述していないことを機械postconditionにせず、instruction実施を同期保証と同一視した。worker/reviewer checklistではなく、親側のcommit後postconditionとfinal HEAD検証で対策する
- stdin PTY transportのcaller-side `stty raw -echo`依存: 責務境界のescaped design issue。byte count・SHA-256・shell quoting回避・PTY実輸送を詳細確認した一方、caller-side recipe込みのtransport成功をglm-worker CLI単体の自己完結contractと取り違えた。対策はTTY/PTY mode変更と元state復元をinvocation-localなglm-worker内部責務へ移し、pipe exact-readとmulti-repository isolationをprocess-level testで固定する
- tracked plan stale-by-one: 親Codex orchestration失敗。planをtask commitへ含める契約、`[x]`は個別commit後だけという契約、各commit直後にplanを更新する契約を同時に適用し、同一commit完了をcommit前planへ書けず、commit後HEADの現在作業が一世代古くなる自己参照を解消しなかった。対策はcommit-ready planを含む初回commit後、停止・報告・install・別taskを挟まず親Codexが完了証跡と次taskへplan/historyを同期し、同じcommitへ即時amendしてfinal HEAD/cleanを確認する二段階contract。大規模ledger・別status DB・worker/reviewer checklistは追加せず、親用git instruction/EVAL/wiring testへ固定して同commit自身で実行済み
- installer preflight fail-open: `glm-worker`内部pipeline/review失敗。`if ! (...)`条件文脈でsubshell内errexitが抑止され、途中の`go test`失敗が最後の成功に上書きされた。旧smokeは同じcompile errorでtestと後続buildを両方失敗させたため、preflight続行後もbuild段階で停止して配置不変assertが通り、元のfail-openを検出できなかった。対策はsubshell各commandとtop-levelの明示失敗伝播、およびtest/build4段階をgo shimで独立発火して後続呼出ゼロまで固定する単一contractとした
- 複雑性の責任評価: escaped review起点で追加されたguard・scenario・parser複雑性はCodexだけの自発的拡張とみなさず、外部レビュー要求、親Codexの局所修正判断、GLM実装/reviewer検証の各寄与を一次証拠で分ける。対策は個別checklist増殖よりcross-cutting invariantと単一contractへの収束を優先する
- task corpus migration未確認のfalse-complete: 親Codex orchestration / requirement preservation失敗。恒久RULESへlossless Original、priority/dependency分離、conditional quality gate等を反映し、一部の重要task metadataを修正した時点でmigration完了扱いしたが、既存未完了task corpus全体が新RULESへ適合したことをpostconditionとして確認しなかった。source-of-truthをRULESへ分離した結果、RULESと既存taskの一時的不整合が発生し得るmigration問題自体をreview対象にしていなかった。対策は個別checklist増殖ではなく、rule変更が既存tracked taskへretroactiveに影響する場合にcorpus migration要否を明示判定し、全未完了taskが同じcontractで実行可能なことを機械確認してから完了するcross-cutting ruleとする

## 完了済み

- [x] Task 009で保存済みtelemetryだけを読むread-only `--call-outliers`を追加し、task/phase/session/model別のworker呼出分布、current/resume、task単位のcall・turn・duration増幅、p95 outlierをmachine JSONで再現可能にした。worker phase category、線形補間、母数20未満のineligible、media-backup相当の小母集団、prompt/response非漏出、empty/partial telemetryを固定。親Codex最終reviewで、telemetry directoryの非ENOENT I/O failureがnormal noneへ潰れる境界と、zero-only taskがtask p95母集団を水増しする境界を検出し、同一sessionで`os.ReadDir` fail-closedと`turns_observed_calls`監査field・観測済みtask限定へ修正した。全test/race/vet/build/gofmt、独立review、Sol最終採否、parent acceptを完了。GLM消費はprovider/wall-clockの二次観測であり、Codex Reduction・Quality Deltaの直接証拠にはしていない

- [x] Task 015でwrapper対象4 caseのoffline contract/wiringを現物照合し、Z.ai 5時間limit early-stop incidentを既存external feasibility gateへ統合。producer field/schema/timingは実producer最小PoCをproduction実装より先に要求し、人工fixture・scripted packet・worker/reviewer/Sol合意を成立証拠にしないauthorityをinstruction/EVAL/wiring testへ固定した。自然なpositive behavioral evidenceはsafe interruption taskのOriginal instructionとcommit `aec9c26`の実装前境界、実Claude CLI 2.1.226 process停止・session resume PoC commit `7aea886`、親Go判断後のproduction commit `f083722`の順序で確認し、追加synthetic AI callなしで再発防止を完了判定。install smokeはGo testだけへcaptured help canaryを注入し、親Claude probe・`--help`単独assertionを維持したまま後半caseまで真exit 0へ復旧。全test/race/vet/build/gofmt、独立review、親Codex差戻し後の同一session修正・再review、Sol最終採否、parent acceptを完了
- [x] Greptile初回日次reviewの境界を訂正。時刻未指定のdaily automationを毎日09:15 JSTへ固定し、初回`b6442c2..6e2cdeb`をStandard CLIで実行してfinding 0件・terminal successを確認後、checkpoint refを`6e2cdeb`へ通常fast-forwardした。続いて親Codexだけに許可された通常fast-forwardでremote mainも同SHAへ進めた。今後のreview範囲はlocal HEADではなくremote `refs/heads/codex/greptile-reviewed..refs/heads/main`の2 refを唯一の正とし、scheduled run開始時はlocal main/HEADがremote mainと完全一致する場合だけCLIを実行する。scheduled run自身はmainをpushせず、正常review時だけcheckpoint refを進める
- [x] Greptile日次外部reviewをglm-worker production integrationへ広げず、Codex scheduled task＋既存git/Greptile CLIの直接運用として設定。誤scopeのGLM callは`--stop`で安全停止しsource変更なしを確認、subprocess wrapper・Go type/state・review lifecycle・machine interfaceを追加していない。訂正済み`b6442c2`をfull SHA `b6442c2a2821e245f371c44f4aeedff958f007d1`へ解決しcurrent HEAD ancestorを確認後、親Codexだけに許可されたpushでremote単一authority `refs/heads/codex/greptile-reviewed`を初期化。Codex automation `greptile-review`を1日1回で作成し、current branch・checkpoint ancestor・HEAD進行をfail closed確認して条件成立時だけStandard `greptile review -b <SHA> --json`を1回実行する。dirty working treeはreview対象外として許容し、開始時branch/HEADを固定、finding確認もcommit済みdiff/blobだけを使い、review中にbranch/HEADが動けばcheckpointを進めない。正常完了時だけ通常fast-forward、quota/CLI/Greptile/precondition failureはref不変で次回catch-up、findingはCodex最終判断、自動課金/修正/force/GLM push禁止をpromptへ固定。parent-maintenance metadataだけを親Codexがcommitし、production source pushなし
- [x] 停止taskを保持した割り込みtask checkout/state隔離を実装。user interruptionによる`interrupted` taskだけを対象に`--isolate`が別git worktree/branchとpath由来の別state slotを作り、元repo・隔離側へtask/repo/branch/HEADの対称なmachine記録を保存する。再`--isolate`は同じ隔離先を冪等再観測し、stale・破損・別task記録は上書きせずfail closed。停止時git 3軸snapshot、親管理metadata除外dirty/untrackedのpath/hash、tracked binary patchを固定し、resume前にbyte保持とHEAD祖先関係を照合する。非親管理HEAD前進は記録branch tipの通常merge統合・元task/repo/作成HEAD・対称な出自記録を実質検証できた場合だけ承認し、squash/cherry-pick・未統合・非対称・保持対象変化はstate/checkpoint/sessionを残して0 model callで拒否する。Plan/TASKS付き実binary lifecycle、元state byte保持、再実行、統合後resume、stale/破損行列、全test/race/vet/build/gofmt、独立review、親Codex差戻し4件の同session修正・再review、Sol最終採用を完了。untracked本文は保存せず親が保持する原本なしには復元不能、旧interrupted checkpointはmigrationせずfail closed、GLM commit/pushなし
- [x] safe interruption production実装前のexternal feasibilityを実Claude CLI 2.1.226で確認。独立process group PID/PGID 49940上の実Bash tool開始後にTERMし、wait exit 143、group probe ESRCH、process残存0、停止後sentinelなしを確認。同じpartial session ID `ab79a6e3-5599-4d57-aa0b-a486c4b6078c`を公開`--resume`で1回だけ再開し、session一致・`RESUME_OK`・中断Bash非継続を確認した。process safe-stopと別task checkout/state隔離を別責務へ分離し、前者は単一目的local stop handshake・process-group cleanup・interrupted checkpoint/statusとしてSol Go、後者は別checkout第一候補の独立taskとした。production変更なし、追加callは中断1回とresume確認1回に限定
- [x] external-review follow-upで永続task.statusの任意値がmachine contract外へ漏れる経路を修正。`--status`・`--stats`・`--timeline`・`--convergence`を現行6値+nullへ単一helperで収束し、未知永続値・未知archive値をraw JSON nullへ正規化。全4 producerの既知/未知contract test、README、全test/race/vet/build/gofmt、独立reviewの残存producer指摘後の同一session修正、Sol採否、parent acceptを完了。GLMを安全停止したつもりでもorphan childが別taskへ書込み続けたincidentは、process停止とtask保持を設計する次ACTIVEへlosslessに分離。親の反復手作業を実装コストに見合う場合は改善taskへ追加する恒久product化判断もRULESへ固定した
- [x] Task 008で旧KEY行とmachine JSONを追加AI callなしの同一semantic payloadで比較。保存済み実corpus22件＋synthetic 9件に対し、machine JSONはstdout bytes +0.98%、run token proxy +0.79%、悲観char-punct proxy +6.30%で削減効果を示さなかった一方、意味保持31/31（旧形式27/31）、placeholder noise 0、Era C result correction 0（n=7 task、傾向のみ）を確認した。Direct Codex/Sol A/Bは未実施のまま分離し、Codex Reduction実証とは扱わず、単一machine contractと情報保持のQuality Deltaを根拠に継続採用。再現可能testdata/artifact生成、全test/race/vet/build/gofmt、独立reviewの説明文2件修正、Sol採否、parent acceptを完了。reviewer自身のrepository内build artifactでreview-end snapshotが変化した事象はsource外部変更と分離し、artifactを退避して同session修正・再reviewした
- [x] Z.ai 5時間Usage Limit early-stopを実producer PoC後にNo-Goへ訂正。Claude CLIのretry attempt 1-10はgeneric 429だけを公開し、exact `[1308]`・5h・reset時刻は全retry後のterminal assistant/resultで初めて公開するため、commit `cbf71c7`の早期停止はwall-clockを短縮せずterminal resultを失わせると確認した。`CLAUDE_CODE_MAX_RETRIES=0`やretry ownership移管は最上位Evalへの利益よりtransient回復の複雑化riskが大きいとして採用せず、early-stop production code/test 5 fileだけを`cbf71c7^`へ選択的に逆適用。Plan/History/task lifecycleは巻き戻さず、terminal 5時間limit分類、checkpoint/session、RATE_LIMITED、reset時刻、auto-resumeを維持し、関連test・全test/race/vet/build/gofmtを完了。新しいGLM実装/review callは追加していない
- [x] completion detection反復false negative重大インシデントを是正。producer/telemetry/親tool output各1件とDesktop可視2件を分離し、model context二重流入なし・actual token影響unknownを維持。単発live positive・模擬fixed Eval・instruction文面を継続的保証とした完了証拠を撤回し、authority区別をwiring testへ固定。Desktop表示はCodex Reduction/Quality Delta実害の新証拠または調査可能なDesktop修正境界が得られた場合だけ再開するBLOCKED taskへ保持。全test/race/vet/build/gofmt、独立review、Sol最終採否、final HEAD gate、本配置、managed instruction一致を完了
- [x] 再openしたTask 007でglm-worker外部machine contractをJSON/JSONLへ統一。通常task結果を含む単発成功はtyped JSON、単発失敗は`error.kind/message/detail` JSON + non-zero exit、継続watch・warning・PTY stdin-ready controlはJSONLへ収束した。後続external-review follow-upで、status/status statsの観測不能sentinelをnull、stats全mapのzero-dataを`{}`へ固定し、watchの非ENOENT I/O failureをstructured error + non-zeroへ分離した。ASCII graph・KEY行・yes/no/none presentation・display parser/projection・dual/fallbackを削除し、READY-before-write、checkpoint v4、PacketCompactions historical、transient recovery等を維持。全test/race/vet/build/gofmt、独立review、Sol最終採否、final HEAD gate、本配置、installed machine JSON smokeを完了
- [x] machine-only backward compatibilityとlegacy migrationを棚卸し・削減。argv `--decision` / `--fix`をusage errorへfail closedしstdin transportへ統一、v2/v3 resume checkpoint migrationとphase/suffixによるreport-only推定を撤去した。resume checkpointをv4へ上げて`report_only`をfalseでも明示保存し、loaderはcurrent v4だけを受理、key欠落・非bool・旧versionをrouting前に拒否してworker/probe 0 call・status不変を固定した。旧protocol比較に必要なstats v3 `PacketCompactions` historical decode/aggregate/outputは維持しdead producerだけを削除。inventory artifact、README/instruction同期、全test・race・vet・build・gofmt、独立review、rate-limit同一session resume、Sol指摘後再review・最終採否完了

- [x] Task 006外部review follow-upとして、NEEDS_SOL_REVIEW固有の`sol_question`をreviewer共通fieldへ加えるstatus別contract field集合へ統合し、validator・MachineJSON・Displayの単一の正へ収束。改行/CR・1536 bytes超拒否、上限ちょうど受理、空/空白拒否、PASS/FIX_REQUIRED noise非検証・非出力、全5 statusのvalidator/output集合直接照合を固定。Task 007の未コミット変更をtask固有stashへ分離したまま、全test/race/vet/build/gofmt、独立review、Sol最終採用、parent outcome記録、final HEAD gate、本配置、installer条件でのsource/installed binary一致、installed `--status` smokeを完了
- [x] Codex-facing result protocolを旧KEY行表示からstatus別契約fieldだけのcompact 1行JSONへ移行し、最終stdout・reviewer/auto-fix prompt・last-review stateを同一machine表現へ統一。人間向け`Display`は診断projectionとv2 checkpoint decoder対として分離した。field auditで検出したPASSのnoiseを根拠に`sol_question`を`NEEDS_SOL_REVIEW`だけへ限定し、`sol_packet_bytes`を親へ実際にemitしたaccepted result payload bytesというformat非依存のstable semanticへ固定。bytes/token proxy比較、roundtrip・status-scoping・production emit exact-byte test、全test/race/vet/build/gofmt、独立review、Sol差戻し後の同一session修正・再検証、parent outcome記録、final HEAD gate、本配置、installer条件でのsource/installed binary・managed packet instruction一致、installed `--status` smokeを完了
- [x] Claude CLI compatibility preflightのPoCを追加AI call・production変更なしで実施。runner/probeが渡す21 flagとoutput-format語彙を棚卸し、2.1.226 helpのbracket圧縮表記を誤拒否しないtest-only checker、欠落featureごとのfail-closed fixture、binary/version/help境界、inventory双方向drift、live no-AI canary、help snapshotを固定。実測overhead約0.25秒、検出不能semantic範囲とfalse reject riskを分離した。Sol判断は最小採用とし、runtime gate・override envは追加せず、低頻度障害時の診断1〜2 turn削減より全task停止riskと運用複雑性を重く評価。全test/vet/build/gofmt、独立review、parent outcome記録を完了。test-onlyのためruntime install対象外。実互換障害・診断turn反復・drift canary失敗をactivation条件とするBLOCKED再評価taskへ判断再読triggerを固定
- [x] 異なる2 Git repositoryでのglm-worker通常並列利用をglobal serializationなしのtracked contractとして固定。実binary process-level testでrepo別state/lock/task/session/checkpoint/telemetry/event/status/reset/rate-limit resume非干渉と同一repo第2processだけのlock拒否を検証し、共有cache rootのrepo-search分離、2実PTYのmode/payload非干渉も追加AI callなしで固定。共有資源8種をread-only shared・namespace済み・upstream管理・per-processへ分類し、provider quota共有をrepo state競合から分離。production code無変更、新coordination機構なし。全test/race/vet/build/gofmt、独立review、rate-limit同一session resume、Sol採用、parent outcome記録を完了。test/docのみのためruntime install対象外
- [x] MODEL_IDLEのmodel activity producer/consumer契約を修復。共有受理集合へassistant thinking/text/tool_useとevent log非保存のsystem/thinking_tokensを含め、live snapshotへmodel activity専用時刻を追加。system/tool_progress・task notification・user tool_result・background通知・resultでは専用時刻を進めずgeneric activity時刻を維持し、旧snapshotはevent log側assistant観測へ安全側fallbackする。production Run・producer/consumer統合・受理集合・旧snapshot test、全test/race/vet/build/gofmt、独立review、Sol採用とparent outcome記録、final HEAD gate、本配置、installer条件でのsource/installed binary byte一致、installed `--status` smokeを完了
- [x] terminal packet後のparent review opportunityとaccepted/fix/decision/unknown outcome、fix origin、差戻し後reworkをraw本文・追加AI callなしで記録し、Codex新規検出(`codex-review`)とGLM reviewer既記載(`glm-reviewer`)を分離。exactly-once・加法整合・旧record未観測・read-only statsをproduction-path testで固定し、独立review・Sol差戻し・同一session修正/review・test/race/vet/build/gofmt、final HEAD gate、本配置、installer条件でのsource/installed binary byte一致、installed `--status` / `--accept` smokeを完了。実装前に発生した当該terminal packetはopen opportunityを持たないため`--accept`は設計どおりno-op
- [x] codex-configとmedia-backupの保存済みglm-worker telemetryを追加AI callなしで横断計測。codex-configは17 task / 62 model call（current raw 58、historical gap 4）、media-backupは4 task / 8 call（coverage complete）として別母集団で評価した。codex-configの`fix_commands=11`は7 taskへ分布するがCodex差し戻し・ユーザー追加・external review・metadata repairのoriginを判別できず、Codex actual usageとQuality Deltaも現telemetryではunknownと確定。packet bytes、parent touchpoint、GLM turn/token/durationをCodex actual usageへ読み替えず、GLM消費は二次制約へ限定した。採用施策は低cardinality parent review outcome telemetryだけを独立task化。Task 009/010前倒し、review縮小、model downgrade、Kimi K3は証拠不足で保留し、Direct/orchestrated live A/Bはpermission待ちを維持。pushなし
- [x] 未レビューcommitで残った3不整合を修正。READYなし即writeの実PTY testでstartup/feed raceを4/4再現後、Sol decisionによりTTY raw適用成功直後・payload read前の`GLM_STDIN_READY` stderr markerをexactly once出し、callerはexact marker確認後だけ本文を1回送る最小handshakeへ収束。非TTY無marker、marker前送信禁止、short read/SHA mismatchのstate/model call前fail closed、termios復元、特殊byte 25輸送、instruction hash/wiringを固定した。task corpus conformanceはfence外top-level Status/Dependenciesだけを判定し、MODEL_IDLEはassistant thinking/text/tool_useだけで更新してtool_progress中も増加。全test・race・vet・build・gofmt、独立review、Sol判断後の再reviewと最終採用を完了。GLM commit/pushなし
- [x] Parent Codex / GLM worker / reviewerの通常固定contextを実wiringから監査し、意味契約を維持して3 role合計42,874→41,734 bytes（-1,140）、同一token proxy 12,089→11,767（-322）へ削減。優先対象`glm-execution.md`は13,888→13,108 bytes（-780）で事故発生日・過去実機確認・raw/noecho/termios等CLI内部詳細をHistory/README/testへ移し、caller固定contractを維持。WORKERは毎call動的`REPORT_ARTIFACT_DIR` blockとの重複を除去して9,898→9,538 bytes（-360）、REVIEWER/AGENTSは大規模rewriteせず不変。wiring inventoryとbefore/after artifact、manifest/contract pin、全test/race/vet/build/gofmt/install smoke、独立review、Sol品質gate、final HEAD gate、本配置、installed/source一致、installed `--status` smokeを完了。新context機構なし、GLM commit/pushなし
- [x] `glm-worker --watch --verbose`へ長時間自走用live tool observabilityを追加。plain `--watch`の表示・終端・retention・event JSONL schemaを維持し、verbose時だけTASK_AGE、MODEL_IDLE、CURRENT tool/elapsed、Bash COMMAND、PURPOSE、BACKGROUND_WAIT、直前の10秒以上tool、直近tool errorを5秒周期と状態変化時に表示。provider stream eventからtool inputを抽出し、command 2KB・purpose 512B上限の0600 atomic current snapshotへ上書きしてcall終端でtoolsを空化し、追記型transcriptとClaude session JSONL依存を追加しない。表示時は200 runeへtruncate。snapshot欠損/破損/write failureはtask本体とevent logへ影響させずdegrade。全test/race/vet/build/gofmt、独立review、Sol品質gate、final HEAD gate、本配置、installed/source一致、installed `--status` smokeを完了。GLM commit/pushなし
- [x] Task 004 stdin PTY transportをcaller-side `stty`不要の自己完結CLIへ移行。stdin payload modeはstate初期化前にTTYを判定し、darwin/linuxではinvocation内でtermiosをraw/noecho相当へ変更して保存状態へ全read/SHA/error pathで復元、ENOTTY以外のprobe failureをfail closed化。raw未実装環境はterminal inputを読取前に拒否しpipe/file exact readを維持。caller instruction/READMEから`stty raw -echo` recipeを撤去し、事前sttyなし実PTYでNUL/CR/Ctrl-C/UTF-8/exact bytes/echo非漏洩/復元、SHA mismatch、short read、pipe、regular file、unsupported境界を固定。全test/race/vet/build/gofmt、独立review、Sol差戻し後再review・最終採用、fulfilled dependency移行、final HEAD gate、本配置、installed/source一致、installed `--status` smokeを完了。GLM commit/pushなし
- [x] Task 001/002 production integration後の不整合を収束。`ExecuteDecision`のACTIVE task解決をdecision消費・state mutation前へ移し、ACTIVE不正拒否後に親修復して同じdecisionを正常再実行するproduction-path testを追加。WORKER/REVIEWERの要求正本をACTIVE task fileのlossless/derived sectionsへ置換し、reviewer二段比較をproduction promptへ固定。ACTIVE/NEXT/BLOCKEDの未知list・説明行をblank以外fail closedとしruntime/installer受理集合と実Git smokeを同期。全task fileから`Status`を撤去してPlanをschedule state単独正とし、完了済みTask 001/003 dependencyを除去して必要invariantをHistorical invariantsへ移行、corpus全体のStatus 0件・dependency現存をtestで固定。全test/race/vet/build/gofmt/install smoke、独立review、Sol品質gate、final HEAD gate、本配置、installed/source一致、installed `--status` smokeを完了。GLM commit/pushなし
- [x] Task 003 TARGETS element semanticのacceptance-set regressionを修正。worker/reviewer全status共有の`validateTargets`へ必須配列長、TrimSpace後非空、重複、`none`小文字・無padding厳密単独正規形、NEEDS_SOL_REVIEWのnone全面拒否、none/具体target混在拒否、FIX_REQUIRED report-only `PACKET`厳密単独契約を集約。empty/whitespace/none/NONE/padding/mixed/concrete/PACKET/duplicateをstatus横断tableで固定し、invalid FIX_REQUIRED/NEEDS_SOL_DECISION/NEEDS_SOL_REVIEWをauto-fix・親dispatch・Sol境界前に補正するproduction test、reject telemetry安定化、prompt/EVAL/manifest同期を完了。瞬断後はworker snapshot SHA-256一致を確認して復旧sessionで全test・race・vet・build・gofmt・独立reviewを再実行し、Sol差戻し2件後の再review・最終採用を完了。final HEAD Plan gate、本配置、installed binary/source managed files一致、installed `--status` smokeを確認。GLM commit/pushなし
- [x] Task 002 tracked canonical Planのfinal HEAD postconditionをproduction `install.sh`のmanaged配置前gateとして実装。HEAD収録PlanのACTIVE一意性、ACTIVE/NEXT/BLOCKED全bulletのtask path・regular file存在、ACTIVE重複、Git境界、pending過渡表現をfail closed検証し、非Git・commitなし・untracked/HEAD未収録Planだけをskip。installerとruntimeのpath配置・bullet抽出受理集合をparity testで固定し、4cedc91型stale、削除/欠損参照、malformed bullet、境界不一致、同一commit復旧、正当なdirty treeを実Git scenarioで検証。全test・vet・build・gofmt・shell syntax、独立review、Sol差戻し4件の同session修正・再review・最終採用、final HEAD gate通過、本配置、installed binary/source・managed instruction/source一致、installed `--status` smokeを完了。GLM commit/pushなし
- [x] Task 001 requirement/task lifecycleのproduction統合を完了。`IMPLEMENTATION_RULES.md`・Plan・TASKS配下全file・Historyをparent-managed metadataの単一集合としてworker呼出前後guard、review snapshot、停止時checkpoint、review-resume deltaへ一般化し、Plan ACTIVEから一意解決したregular `.md` task fileをtask開始時stateへ固定してworker/reviewer全promptとresumeへ要求source blockを配線。追跡中欠損・呼出中変更・ACTIVE欠損/非一意/path escape/non-regular・旧2file stateをfail closedとし、停止中親deltaだけをfile単位で承認。active-task修復再開、同一fileの呼出中+停止中連続変更拒否、semantic filename、symlink拒否を含むunit/scenario/wiring、全test・race・vet・build・gofmt、独立review、3回のSol差戻し後再reviewと最終採用、本配置、installed binary/source・managed instruction/source一致、installed `--status` smokeを完了。GLM commit/pushなし
- [x] 全未完了task corpusが現RULESへmigration済みであることを再確認し、RULESからTask 022 final gate、Task 021 decision手順、旧Sol記述の読み替え、Task 011/012/014/018由来のevaluation detail、最終installed-state task detailを除去。source-of-truth、parent maintenance、lossless requirement、priority/dependency、再読、ownership、task lifecycle、必要なSol gate、cross-cutting install等の全task共通invariantは維持し、retroactive rule変更時のmigration invariantを1行へ圧縮。ACTIVE Task 001とproduction checkpoint/sessionは変更していない
- [x] 新RULESへの既存未完了task corpus migrationをparent maintenanceで完了。Task 003〜008の既存lossless sourceを再確認し、Task 009〜022とblocked 6件の要約Originalを統合作業指示の該当原文へbackfill。全28 taskのOriginal、Dependencies、固定番号workflow、conditional quality gate、blocked activation、RULES/acceptance整合を機械確認し、dependencyをrepository-relative task pathへ統一。初回整理で完了していたfalse dependency撤去、Task 023廃止とcross-cutting install、Task 022 dynamic gate、Task 021 parent decision gate、evaluation evidence、exhaustive full-corpus、simple repo-search fallback、既存usage metric定義を維持。ACTIVE Task 001とproduction checkpoint/sessionは変更していない
- [x] 新規`IMPLEMENTATION_TASKS/*.md`をsemantic slugだけで命名し、sequence・priority・status・dependency・completion/作成順・permission wait分類をnumeric prefixへencodeしない恒久規則を追加。task受領時の親instructionへsemantic filename生成を明記し、実行順序/priorityはPlan、dependencyは各taskの`Dependencies`を正とした。Planを含むtask scheduling listはunordered marker `-`とsource上の行順を使い、割り込み時の後続renumber差分を除去。現行tree 195ファイルの生成・prompt・testを検索し、番号prefix必須または番号大小による順序/dependency推論がないことを確認。既存29件の番号付きtaskはrenameせず、専用禁止validatorも追加していない。parent Codex review完了
- [x] external reviewで判明したstructured output protocol regressionを修正。旧text protocolのstatus別required fieldsを再照合し、reviewer `PASS`・`FIX_REQUIRED`・`NEEDS_SOL_REVIEW`とworker `NEEDS_SOL_DECISION`の非空`TARGETS`をschema/validator/promptへ復元、対象なしdecisionは`none` sentinel、`IMPLEMENTED`だけ空配列を許容した。schemaが許す未知propertyはGo parserも無害に無視して表示・stateへ非伝播とし、既知field型・必須・semantic違反はfail closedを維持。対応表、producer/consumer acceptance、auto-fix/親dispatch因果、scenario corpus、全test・race・vet・build・gofmt、独立review、Sol指摘後再review完了
- [x] `--stats`へTaskStats model call数とraw JSONL現行Task Work Call record数のcoverage表示を追加。task単位の欠損・過剰を相殺せず、archive済みstats専有をhistorical gap、読取不能をunreadable、集計外telemetry fileをorphanとして分離し、不完全時はusage totalsをunknownと表示する。既知`ccc205d1`型gapを推測補完せず、永続schema・記録経路・既存CLI keyを変更しない。件数一致では同一task内の重複と欠損の置換を識別できない限界は既存exactly-once不変条件と併用。完全一致/0件/不足/過剰/orphan/旧version/unreadable/archive flowのtest、race・vet・build・gofmt、独立review、Sol指摘後再review完了
- [x] review-resumeの全worktree同一性と親Codex必須plan/history更新を両立。review開始時とrate-limit/provider-unavailable停止時の親管理2file hash、親2file除外worktree digest、HEAD/indexを組み合わせ、停止期間中の親更新だけを承認してreview基準を現状へ再固定するcontractを追加した。worker/reviewer実装surface・親2file削除・旧形式state・呼出中変更・crash windowはfail closedを維持し、再固定telemetryを記録。停止中の書込主体は親Codexだけという既存運用不変条件を採用し、任意の並行processの主体認証は残余リスクとして明示。positive/negative/削除/追加/tracked/subdir同名列/legacy/再固定後reviewer変更/crash window scenario、全test・vet・build・gofmt、独立review・HIGH変更のSol品質gate完了
- [x] 別PC引継ぎ前に本repositoryとKindle-automationのGit・task status・raw telemetry/statsを追加AI callなしで照合。本repositoryは37 task・192 Task Work Call、現task4 call/154 turn/記録cost約10.53、直前structured移行task11 call/535 turn/約30.37。Kindle-automationはclean、HEAD `6b2e626`、active task/sessionなしで、旧`e7b17cb0`判断待ちtelemetryは後続Git履歴より古くresume対象外と確定。詳細な再開状態はcommit時点の`IMPLEMENTATION_PLAN.local.md`へ集約
- [x] Codex↔GLM production workflowをrole別typed schemaとauthoritative `structured_output`の単一protocolへ移行。旧marker/KEY parserと構造欠陥recompressionを撤去し、意味検証不合格だけ同一sessionで1回修正、schema retry枯渇・成功result欠損はfail closed、v2→v3 checkpoint変換、provider分類・report-only・risk floor・snapshot・telemetry exact-once・installer preflightを維持した。独立reviewでpreflight未配線を修正し、Sol reviewで`structured_retry_exhausted`への成功result欠損混入を分離後に再review、全test/race/vet/build/gofmt/install smoke完了
- [x] 2026-08-21時点のcanonical v3 stats/raw telemetryを追加AI callなしで分析。36 task・Task Work Call 181、raw JSONL 180 record、総tree usageはinput 11,918,317・cache-read 306,008,960・output 2,655,467、記録cost約278.98。差分1件は2026-08-20の`--help`誤起動でwrapper消失後もClaude processが孤児化した既知task `ccc205d1`で、stats 1 callに対しJSONL 0のhistorical gap。usageは推測せずcoverage incompleteとして扱う必要がある。旧text PACKETはstray-body invalid 39 callを生み、確実な追加負担は後続packet compaction 38 call・約9.04（総cost約3.2%）で、invalid call約66.56全額は実装/review本体を含むため削減見込みへ数えない。worker/reviewer costは約229.55/49.43、reviewer valid終端66件中8件が`FIX_REQUIRED`。worker-new 41 callはturn中央値55・p95 137・cost中央値約2.70・p95約8.87に対し、structured output移行taskのresumed workerは320 turn・約20.08・Bash 211回で明確な外れ値。resumed worker全体は非resumeより平均call cost/turnが低く、現時点で一律session rotationの根拠はない。GLM-4.7は6 call treeのみでmodel routing判断不能、Bash metadataもtest/search/buildを区別しないためtest省略判断不能。優先順はstructured outputによるcompaction除去、telemetry coverage明示、worker outlier観測とtask/milestone境界、sanitized operation category、最後に実Direct/orchestrated A/Bとし、review縮小・model downgrade・hard turn cap・test省略は品質証拠なしに行わない
- [x] Codex↔GLM structured output feasibility PoCをClaude Code 2.1.226・Z.ai Coding Plan・GLM-5.3・stream-json・`--json-schema`の実経路で実施。schema適合output 2/2、同一session resumeとprompt cache continuity、deterministic extraction、token/cost/session metadata保持、invalid JSONのclient側拒否、unsupported schemaのretry exhaustion後fail closed、既存provider分類channel維持を一次artifactで確認しGo判断。現行PACKETとbyte/token proxyは同等だが保存telemetryのformat failure 65件中53件（82%）は構造欠陥であり、typed fieldへSol判断情報を保持したままrecompression call削減余地を確認。productionはGo側schema事前制限・contract preflight・自然発生429再確認・retry cost撤退条件付きとし、PoC中のrepo/PACKET/parser変更なし
- [x] tracked canonical planのstale-by-oneを、commit-ready初回commit→親Codex plan/history最終同期→同一commit即時amend→final HEAD/clean確認の親orchestration contractで解消（独立review・Sol fresh test/vet/build/gofmt/shell syntax・同commitでの実運用完了）
- [x] `ba2414b` BM25 fingerprint domainのfalse-completeを修正し、nested repo・submodule・default/追加exclude・large/binary/symlinkの検索/index corpusとcache freshnessを同一policyへ統一（独立review・Sol設計採否・fresh test/vet/build/gofmt完了、同commitへplan/history同期）
- [x] `glm-worker --watch`をauthoritative task statusのnon-active/task切替時に最終event drain後終了させ、通常は主process、attach recoveryだけwatchを使う親completion wait contractへ修正（独立review・Sol fresh test/vet/build/gofmt・本配置・binary/instruction一致・実non-active taskの`WATCH_EXIT`/exit 0確認完了）
- [x] `install.sh` preflightのtest/build途中失敗を後続成功で上書きせず、最初の失敗でmanaged配置前に停止するfail-closed contractを4段階go shim smokeで固定（独立review・指摘後同session修正・再review・Sol fresh smoke/test/vet/build/gofmt/shell syntax完了、同commitへplan/history同期）
- [x] `IMPLEMENTATION_PLAN.local.md`と`IMPLEMENTATION_HISTORY.md`を親Codex専有のtracked surfaceとしてself-protection/model-call前後guardへ統合し、実行済みTask Work Callをguard失敗を含む全terminal pathでraw telemetryへexactly once記録（独立review・Sol fresh test/vet/build/gofmt完了、同commitへplan/history同期）
- [x] reviewer risk floorをwrapperで強制し、HIGHからPASS/LOWへの降格を拒否
- [x] provider一時障害のcheckpoint、内部backoff、probe、resume可能停止
- [x] worker終了・reviewer開始・reviewer終了のHEAD/index/worktree snapshot固定
- [x] orchestrator自己変更を意味上HIGH化する基礎policy
- [x] prompt/instruction変更の行動scenario基盤
- [x] packet拒否理由、risk floor、Git状態等の診断telemetry
- [x] 自動再開の実体検証、二段階作成、固定Eval
- [x] 特例直接作業許可の非推移化とrepository単位生存判定
- [x] Z.ai managed mappingをGLM-5.3へ更新
- [x] 複数完成PACKETをwrapper拒否し同一session再圧縮へ接続
- [x] provider probeのexit 0 + is_error、固定sentinel、false positiveを厳密検証
- [x] provider recoveryのTask Work / Probe / Resume call accountingを分離し、token・cost・resolved model・durationを修正
- [x] root `AGENTS.md`から実装計画をbootstrapする基礎policyをscenario固定（旧Git管理外運用は後続のtracked canonical source移行で置換）
- [x] provider probe validation失敗後のbackoff、deadline、5h、fatal/auth/config状態遷移をscenario固定
- [x] provider recoveryのtransient→probe→resume→5h limitをcall accounting・checkpoint・final status込みで固定
- [x] provider probeとTask Work/Resume call accounting不整合を固定
- [x] `70533f0`時点のglm-worker・managed AGENTS/instructions/settingsを本配置し、趣味PCのZ.ai default一致を確認
- [x] self-protectionのautoresume、installer、managed settings、config、entrypoint等critical/非対象surfaceを単一policyへmanifest化
- [x] self-protection critical surface漏れを意味ベースscenario/manifestへ固定
- [x] `8c85a62`時点のself-protection判定を本配置し、managed files・趣味PC Z.ai default一致を確認
- [x] 永続状態系escaped case（override解除残留、managed値再流入、invalid null、identifier rename、baseline/state欠損、危険なrecovery説明）をinstaller test・状態遷移scenarioへ固定
- [x] reviewer working tree mutationとmultiple complete PACKETをwrapper scenarioへ固定
- [x] HIGH意味情報不足を`TARGETS: PACKET`で通常implementation auto-fixと分離し、report-only専用production prompt/pathとprompt dispatch因果testを追加（`c06f6b2`、本配置・install smoke完了）
- [x] provider recoveryのprobe成功後resumed Task Work Callがordinary fatalでもraw telemetryへexactly once記録し、stats・Task/Probe加法整合とphantom防止を固定（`b72173e`、本配置・binary一致完了）
- [x] provider probe fatal matcherから裸の`unauthorized`/`forbidden`を除外し、明示文脈だけをfatalとしてcheckpoint/session保持まで固定（`1875e87`、本配置・binary一致・install smoke完了）
- [x] 外部成立性の条件付きfeasibility gateを親Codex contractへ追加し、対象固有観測期間・配線test・wrapper終端例・未実行親behavioral Evalを分離（`5bbcdaf`、本配置・install smoke完了）
- [x] automation/monitor安全停止・GLM child完了・親USER_REQUEST完了を3分類し、明示継続範囲と停止条件を親lifecycle contractへ固定（`e500982`、本配置・install smoke完了）
- [x] 原因不明の外部取得・parser・integration failureで、原因判定に必要なsanitize済み最小evidence artifactだけを条件付き保存し、親routing・判定不能終端・artifact scenarioを固定（`29366bc`、本配置・現物一致・install smoke完了）
- [x] workflow package内のinitial/recovery/probe/resumeとper-call telemetry timestamp/durationを注入clockへ統一し、production source guardとfake-clock経路testを固定（`d9ca442`、本配置・binary一致・install smoke完了）
- [x] escaped bug/review原因分析時だけ、一次証拠から`glm-worker`内部pipeline失敗か親Codex orchestration失敗かを先に分類し、原因層に沿う対策と親behavioral Evalを固定（`104a131`、本配置・現物一致・install smoke完了）
- [x] Codex Direct対orchestratedの同一条件run記録をstrict schemaで検証し、actual usage unknown保持、Codex Reduction、Quality Delta、時間、GLM usageをread-only `--eval-ab`で比較するfixed Eval基盤を追加（`977f94b`、本配置・binary一致・install smoke完了。実Sol High baseline・本番A/Bは未実行）
- [x] 追加AI callなしでClaude stream-jsonを秘密を含まないtask event metadataへbest-effort縮約し、task/role/call/phase識別、read-only `--watch`、result/session/resume semantics維持、plain stdoutのsanitize済みprovider分類継承を実装（`3c47593`、本配置・binary一致・install smoke完了。実provider stream schemaは後続taskの自然な実行で観測）
- [x] 既存event log/checkpoint/telemetryだけから詳細`--status`、ID対応できたtool timing、session agingを算出し、thinking_tokens抑止・call上限・旧task event log retentionを追加（`2852e37`、本配置・binary一致・install smoke完了。task `34a23ab2-ab8a-46f2-babd-e9c76ef823ce`で実stream schemaと肥大化要因を確認）
- [x] 保存済みevent logとtelemetryだけからtask/call/role/phase/tool/timestamp/duration/token/session aging・相対graphを表示するread-only `--timeline [task-id]`を追加し、外部task IDをcanonical UUID v4で検証（`1813636`、本配置・binary一致・install smoke完了）
- [x] `TARGETS: PACKET` report-only workerをReadOnly capabilityでdispatchし、開始前後のHEAD/index/worktree同一性をfail closed強制。rate-limit/provider resumeと旧checkpoint移行時も同一baselineまたはcall前停止を保証（`6ec9f8f`、本配置・binary一致完了）
- [x] `--eval-ab`をwrite-capable state初期化前の`AttachStateStore()`経路へ移し、state directory非作成・既存`repo-root`内容/mtime/file構成不変・runner未構築をproduction entrypoint testで固定（`a032e75`、本配置・binary一致完了）
- [x] 完了済み`[x]`を当時のcode・Git履歴・後続escaped bug・実運用結果・全testへ照合し、PACKET二重表示と`f521ab4` doc taxonomyを再オープン（後に修正完了）。後続外部レビューで`ba2414b` fingerprint domainと`962fb20` plan guard telemetryの新たなfalse-completeが実証されたため、該当項目は再度未完了へ移動
- [x] Sol decision本文が途中で欠損した親→worker伝達境界を一次証拠で特定し、長文payload完全性を機械保証（`1dbfda5` stdin専用mode・byte長/任意SHA-256照合・state前fail closed・argv mode後方互換、`3c263a6` 親caller tty:true+`stty raw -echo`+1 write contract・実PTY輸送test。独立review・Sol fresh `go test -count=1 ./...`/vet/build/gofmt・`install.sh`本配置・本配置instruction hash一致・installer条件でのbinary byte一致・live末尾改行なし9-byte payload 1回writeでechoなし全byte到達・意図的SHA不一致のstate/model call前fail closed確認完了）
- [x] `f521ab4` convergence分類のfalse-completeを修正し、doc追加・変更・削除を独立`doc-change`として観測、behavior-critical文書をcomment/format-onlyへ落とさず、code comment/format-only分類を維持（`7105377`、独立review・Sol fresh test/vet/build/gofmt完了。convergenceは観測専用・review省略gate未接続）
- [x] `IMPLEMENTATION_PLAN.local.md`をGit管理するtracked canonical sourceへ移し、親Codexだけが本文・`[x]`・優先順・現在状態を更新する基礎contractを`962fb20`へ収録し、本配置・binary一致完了（worker guardのafter-read telemetry欠落とplan自身のstale-by-one運用は未完了へ分離）
